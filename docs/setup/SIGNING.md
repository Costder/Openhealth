# Signing Key Policy

- Signing key is stored in maintainer keychain, never in repository.
- Rotation requires a migration notice in `CHANGELOG.md`.
- Release keys must not be committed to git history.
