# Install Guide (MVP Scaffold)

1. Build debug APK: `./gradlew :app:assembleDebug`
2. Sideload on Android API 28+ device.
3. On first launch, complete Health Connect availability and permission flow (to be completed in milestone implementation).
4. Manual-only mode is always supported.
